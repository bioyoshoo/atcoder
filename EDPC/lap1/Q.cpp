#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N; cin >> N;
    vector<int> H(N);
    for (int i = 0; i < N; i++) cin >> H.at(i);
    vector<ll> A(N);
    for (int i = 0; i < N; i++) cin >> A.at(i);
    
}