#include <bits/stdc++.h>
using namespace std;

int main() {
    int Q;
    cin >> Q;

    for (int i = 0; i < Q; i++) {
        int q;
        cin >> q;

        if (q == 1) {
            int64_t x, c;
            cin >> x >> c;
                
        }
    }
}