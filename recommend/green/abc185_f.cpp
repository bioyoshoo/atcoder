#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, Q;
    cin >> N >> Q;
    vector<ll> A(N);
    for (int i = 0; i < N; i++) cin >> A.at(i);

    ll extra_sum = 0;
    for (int i = 0; i < Q; i++) {
        ll t, x, y;
        x--; y--;
        cin >> t >> x >> y;
        if (t == 1)
    }
}